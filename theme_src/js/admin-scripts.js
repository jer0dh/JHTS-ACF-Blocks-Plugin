(function ($) {

    /* javascript code goes here.  Will run after page is loaded in the DOM */


    $('document').ready(function () {


        //removeIf(production)
        console.log('starting admin scripts.js');
        //endRemoveIf(production)

        // JS to run in Editor
        if( window.acf ) {
            //    Action to run myFunction whenever  ajax updates any block.
            //    window.acf.addAction( 'render_block_preview', myFunction );
        }
    })

})(jQuery);
